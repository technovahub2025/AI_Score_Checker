services:
  - type: web
    name: ai-score-backend
    env: node
    plan: free
    rootDir: AI_SCORE_BACKEND
    buildCommand: npm install && npx playwright install chromium
    startCommand: npm start
    autoDeploy: true
